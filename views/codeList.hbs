<!DOCTYPE html>
<html>

<body>
  <select id="code" name="code" data-city-code="{{cityCode}}" data-sub-code="{{subCode}}" data-sub2-code="{{sub2Code}}">
    <option value="">리 전체</option>
    {{#each codeList}}
    <option value="{{this.code}}">{{this.name}}</option>
    {{/each}}
  </select>
</body>

</html>